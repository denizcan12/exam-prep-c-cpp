# Review Notes

The following original files were intentionally left out of this publish-ready copy:

- `C_Exam_Mixed_needstobesorted/recursion_Exam6.c` - contains plain Turkish explanation text outside comments, so it does not compile as C.
- `C_Exam_Mixed_needstobesorted/recursion_Exam7.c` - contains an obvious syntax error in an `if` condition.
- `C_Exam_Mixed_needstobesorted/recursion_exam23.c` - contains duplicate `implode_equals` implementations.
- Files whose names start with `!` - these look like duplicate/draft variants and are better handled in a later cleanup pass.
