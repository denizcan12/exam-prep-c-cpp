/*
Exercise: Clone Even

Write clone_even, which receives the head of a linked list and returns a newly
allocated list containing only the even values from the original list while
preserving their order.

Function signature:
node* clone_even(node const* L);
*/
