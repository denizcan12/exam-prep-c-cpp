# Password Vault Project

An early multi-file C++ project structure for a password-vault model. This folder is kept as a project-in-progress rather than a finished application.

This folder keeps the original exercise files and naming style where possible. Generated binaries and local machine files are excluded from the repository.
